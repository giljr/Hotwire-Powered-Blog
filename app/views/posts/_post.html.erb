<div id="<%= dom_id post %>">
  <strong>Title:</strong>
  <%= post.title %>
  <br>

  <strong>Body:</strong>
  <%= post.body %>
  <br>

  <strong>Update at:</strong>
  <%= time_tag post.updated_at, "data-local": "time", "data-format": "%B %e, %Y %l:%M %P"%>
</div>
